dist: trusty
sudo: required

language: c++

compiler:
  - gcc

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libelf-dev cmake3
  - cmake --version

before_script:
  - mkdir BUILD
  - cd BUILD
  - cmake ..

script: make
